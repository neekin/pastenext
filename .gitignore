node_modules
dist
src-tauri/target
src-tauri/gen
*.local
.DS_Store

# 编辑器 / 工具产生的本地目录
.deveco/
.workbuddy/

# 授权密钥:发号用的生产密钥绝不能进仓库(见 .workbuddy/artifacts)
.env
.env.license

# 纯静态注册机(前端版):本地/内用,密钥在浏览器里,故意不进仓库
keygen-web/

# OCR 辅助二进制:由 build.rs 从 ocr/ocr.swift 编译,不进仓库
src-tauri/ocr_helper
